import { Router } from "express";
const habitacionRouter = Router();

habitacionRouter.get("", (req, res) => {});
habitacionRouter.post("", (req, res) => {});
habitacionRouter.put("/:id", (req, res) => {});
habitacionRouter.delete("/:id", (req, res) => {});

export default habitacionRouter;
